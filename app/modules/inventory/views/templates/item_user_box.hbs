<a class="user" href="{{user.pathname}}" tabindex="0">
  <div class="userBox">
    <img class="profilePic" alt="{{user.username}} avatar" src="{{imgSrc user.picture 48}}">
    <span class="user" href="{{user.pathname}}">
      <strong class="username">{{user.username}}</strong>
    </span>
  </div>
</a>