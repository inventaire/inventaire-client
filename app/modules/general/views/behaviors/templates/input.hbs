{{!-- to add success check capabilities, pass 'check' as a 2nd argument (see input.js) --}}
<form id="{{id}}" class="inputGroup">
  {{!-- a div is required to wrap the input as the alert-box
  will be appened as a sibbling node --}}
  <div class="inputBox">
    <input type="{{field.type}}"
    id="{{field.id}}"
    name="{{field.name}}"
    class="{{field.classes}} has-alertbox enterClick"
    value="{{field.value}}"
    placeholder="{{field.placeholder}}{{field.dotdotdot}}"
    aria-label="{{field.name}}"
    {{{special}}}>
  </div>
  <button id="{{button.id}}" class="button {{button.classes}}">
    {{{button.text}}}
    <span class='loading'></span>
  </button>
</form>