{{#with hash}}
  <a href="{{href}}" id="{{name}}IconButton" class="iconButton {{name}}
  {{#if counter}}icon-with-counter count-{{counter}}
  {{else}}icon-without-counter{{/if}}"
     title="{{i18n title}}">
     {{icon icon}}
     <span class="counter">{{counter}}</span>
  </a>
{{/with}}
