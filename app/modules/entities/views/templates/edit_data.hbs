<div class="edit-data-buttons">
  {{#if edit}}
    <a href="{{edit}}" class="showEntityEdit pencil" title="{{i18n 'edit data'}}">
      {{icon 'pencil'}}
    </a>
    {{#if wikidata}}
      {{iconLink 'barcode' wikidata.url i18n='see on Wikidata' linkClasses='showOnWikidata'}}
      {{#unless hideRefreshButton}}
        <a class="refreshEntityData" title="{{i18n 'refresh Wikidata data'}}">{{icon 'refresh'}}</a>
      {{/unless}}
    {{/if}}
  {{/if}}
  {{#if showDeduplicateEntityButtons}}
    <a class="deduplicateEntity" title="{{i18n 'deduplicate entity'}}">{{icon 'th'}}</a>
    <a class="deduplicateSubEntities" title="{{i18n 'deduplicate sub-entities'}}">{{icon 'compress'}}</a>
  {{/if}}
</div>
